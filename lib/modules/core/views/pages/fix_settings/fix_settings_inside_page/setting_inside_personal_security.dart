import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

class FixSettingPersonalSecurity extends ConsumerWidget{
  const FixSettingPersonalSecurity({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("개인 / 보안"),
      ),
      body: Column(
        children: [

        ],
      ),
    );
  }
}
